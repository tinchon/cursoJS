/*Math clases son funciones qe puedo poner adentro
-----random devuelve un numero entre 0 y 9999999 aleatorio
-----floor (redondea hacia abajo) devuelve los milisegundo del momento que ejecutamos la operacion y aplica operaciones matematicas
-----ceil (redondea hacia arriba)
-----round(entero mas cercano)
*/


function mostrar()
{
	var max = 10;
	var min = 1;
		//Genero el número RANDOM entre 1 y 10 
	//var num = math.floor(match.random()*((maximo+1)-minimo)+minimo);
	var num = math.floor(math.random()*((max+1)-min)+min);
	alert (num);


}//FIN DE LA FUNCIÓN